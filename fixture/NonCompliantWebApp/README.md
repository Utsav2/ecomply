# NonCompliantWebApp

A small, deliberately non-compliant web application used as the demo fixture
for the compliance harness. Flask backend, static JS frontend, container
deploy. Do not deploy this anywhere real.
